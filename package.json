{
  "name": "gridwarp",
  "version": "1.0.0",
  "description": "Scalar-warp grid demo with bicubic interpolation",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit && npm run lint",
    "start": "tsx src/server/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "node --import tsx --test test/**/*.test.ts",
    "coverage": "node --import tsx --test --experimental-test-coverage test/**/*.test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.6.0",
    "eslint": "^10.2.1",
    "globals": "^17.5.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.0"
  },
  "dependencies": {
    "noise_generator": "file:../noise_generator",
    "prettier": "^3.8.3"
  }
}
